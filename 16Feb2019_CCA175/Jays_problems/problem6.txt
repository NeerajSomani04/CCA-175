sqoop import \
--connect jdbc:mysql://localhost/retail_db \
--username retail_dba \
--password cloudera \
--table orders \
--target-dir /user/cloudera/jays_problems/problem6/orders/ \
--as-textfile \
--fields-terminated-by '\t' 

sqoop import \
--connect jdbc:mysql://localhost/retail_db \
--username retail_dba \
--password cloudera \
--table order_items \
--target-dir /user/cloudera/jays_problems/problem6/order-items/ \
--as-textfile \
--fields-terminated-by '\t'


val orderRDD = sc.textFile("/user/cloudera/jays_problems/problem6/orders")
val orderFilter = orderRDD.filter(x => x.split("\t")(3) == "CLOSED").filter(x => x.split("\t")(3) == "COMPLETE").map(x => (x.split("\t")(0).toInt

